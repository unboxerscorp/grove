{
  "name": "@grove/dev-room-web",
  "private": true,
  "version": "0.27.0",
  "description": "Grove Dev Room — standalone single-page app served by the grove web server at /. No external plugin host.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "verify": "node verify.mjs",
    "e2e": "node e2e/api.mjs",
    "e2e:all": "npm run build && npm run verify && npm run e2e",
    "check": "npm run typecheck && npm run build && npm run verify"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.27.7",
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.7.3"
  }
}
