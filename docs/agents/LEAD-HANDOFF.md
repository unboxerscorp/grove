# grove-dev LEAD 인수인계 (Claude → Codex)

> 작성: 2026-06-05, 기존 Claude lead(dev10:0.0). 사용자가 신뢰 상실로 lead orch를 Codex 타입으로 교체 지시. 이 문서는 새 Codex lead가 문맥 손실 없이 즉시 이어받기 위한 것.

## 2026-06-06 현재 운영 업데이트 (Codex master)

이 섹션이 아래의 과거 인수인계보다 우선한다.

- 2026-06-06 15:27 KST Slack queue runtime observability:
  - 최신 product-code HEAD는 `e0a0962 fix: expose slack queue runtime metrics`다. Slack routed chat queue의 backpressure를 SQLite 직접 조회 없이 `~/.grove/dev10/slack-runtime.json`에서 볼 수 있게 했다.
  - runtime payload는 `node_chat_queue` object를 항상 포함한다. 안정 필드는 `total`, `pending`, `running`, `failed`, `oldest_pending_age_seconds`이며, 빈 큐도 명시적으로 `0/0/0/0`과 `oldest_pending_age_seconds:null`을 기록한다.
  - 이 변경은 관찰성 전용이다. queue cap, drop, retry policy, routing behavior는 바꾸지 않았다. advisor/watch는 row count보다 `oldest_pending_age_seconds`를 우선 backpressure 신호로 보면 된다.
  - Slack service만 재시작했다. runtime pid `87525 -> 98577`, `socket_connected=true`, heartbeat fresh. main web `8765 started_at=1780721628`과 remote web `5173 started_at=1780721854`는 재시작하지 않았고 둘 다 health ok다.
  - live 확인: `slack-runtime.json`의 `node_chat_queue={"total":0,"pending":0,"running":0,"failed":0,"oldest_pending_age_seconds":null}`, DB queue `total/pending/running/failed=0/0/0/0`, board residue `tasks/comments/p2-test=0/0/0`, `grove org --json` 5노드 전부 `pane_exists=true`, tmux windows `0/1/2/3/4`.
  - 검증: targeted `test_store.py -k slack_chat_queue_summary` green, targeted `test_slack.py -k "slack_main_connects_polls_and_closes_socket or chat_routing"` 16 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 462).

- 2026-06-06 15:18 KST Slack queue restart-crossing reclaim guard:
  - 최신 product-code HEAD는 `84a9480 test: guard slack queue reclaim`이다. 런타임 변경 없는 test-only guard다.
  - Slack queue worker/restart 도중 item이 `running` 상태로 남아도 `SLACK_NODE_CHAT_RUNNING_STALE_SECONDS` 이후 `list_due_slack_chat_messages`가 다시 due로 잡는 경로를 테스트로 고정했다.
  - 핵심 교차점도 함께 검증한다. 이미 `response_text`가 저장된 running item은 stale 후 reclaim되면 cached response를 Slack thread에 delivery하고, master ask/chat facade를 다시 호출하지 않는다. 즉 gap A의 멱등 delivery와 restart reclaim이 같이 유지된다.
  - stale 전에는 due로 잡히지 않아 double-claim을 방지한다. `SLACK_NODE_CHAT_RUNNING_STALE_SECONDS > GROVE_CHAT_TIMEOUT_SECONDS * 2` assertion으로 정상 ask timeout보다 충분히 긴 reclaim window도 고정했다.
  - 검증: `uv run --project bridge pytest bridge/tests/test_slack.py -k "reclaims_stale_running_item or chat_routing"` 15 passed, `uv run --project bridge pytest bridge/tests/test_slack.py` 94 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 461). 런타임 변경이 없어서 Slack/web 재시작은 하지 않았다.

- 2026-06-06 15:12 KST Slack socket wedged self-restart 보강:
  - 최신 product-code HEAD는 `01782f4 fix: restart wedged slack socket`이다. Slack connector process가 살아 있지만 Socket Mode가 60초 연속 disconnected 상태이면 `Slack socket wedged >60s; exiting for fresh restart` 로그를 남기고 종료한다.
  - transient disconnect는 기존처럼 같은 process 안에서 per-poll reconnect를 시도한다. reconnect가 성공해 `socket_connected=true`가 되면 disconnected timer는 즉시 reset된다.
  - 목적은 같은 SDK socket client가 wedged된 채 process만 살아 있어 `run-slack-loop.sh`의 exit 기반 restart가 걸리지 않는 실패모드를 닫는 것이다. 종료 후 wrapper가 fresh socket client로 재시작한다.
  - Slack service만 재시작했다. runtime pid `83004 -> 87525`, `socket_connected=true`, heartbeat fresh. tmux window `2 slack` pane은 유지됐다.
  - main web `8765`와 remote web `5173`은 재시작하지 않았고 health ok다. queue/residue는 `total/pending/failed=0/0/0`, `tasks/comments/p2-test=0/0/0` 유지.
  - 검증: `uv run --project bridge pytest bridge/tests/test_slack.py -k "reconnects_disconnected_socket or sustained_disconnected_socket or chat_routing"` 16 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 460), `grove org --json` 5노드 전부 `pane_exists=true`.

- 2026-06-06 15:05 KST Slack response delivery no-drop 보강:
  - 최신 product-code HEAD는 `ac0bc39 fix: retry slack response delivery`다. Slack routed chat queue가 master 응답을 받은 뒤 Slack thread 게시에 실패하면, 이제 queue item을 `done` 처리하지 않고 pending으로 되돌린다.
  - `slack_chat_queue.response_text` 컬럼에 생성된 응답을 저장하므로 delivery retry가 master에게 같은 ask를 다시 보내지 않는다. 즉 일시적 Slack API/post 실패는 답변 유실 없이 같은 응답을 thread delivery만 재시도한다.
  - live DB migration 확인: `slack_chat_queue`에 `response_text` 컬럼 존재, `queue_total/pending/failed=0/0/0`, board residue `tasks/comments/p2-test=0/0/0`.
  - Slack service만 재시작했다. runtime pid `75126 -> 83004`, `socket_connected=true`, heartbeat fresh. tmux window `2 slack` pane은 유지됐다.
  - main web `8765`와 remote web `5173`은 재시작하지 않았고 health ok다. `8765 started_at=1780721628`, `5173 started_at=1780721854` 유지.
  - 검증: `uv run --project bridge pytest bridge/tests/test_slack.py -k "chat_routing"` 14 passed, `uv run --project bridge pytest bridge/tests/test_slack.py bridge/tests/test_store.py` 111 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 459), `grove org --json` 5노드 전부 `pane_exists=true`.

- 2026-06-06 14:56 KST Slack/web/org soak 샘플:
  - `46f5a78` Slack timeout retry 배포 후 2회 연속 샘플(14:54, 14:56 KST)에서 Slack pid `75126` 유지, `socket_connected=true`, heartbeat fresh.
  - `slack_chat_queue`는 `total/pending/failed=0/0/0`, live board residue는 `tasks=0`, `comments=0`, `p2-test=0` 유지.
  - main web `8765`는 `started_at=1780721628` 유지하며 uptime 상승, remote web `5173`은 `started_at=1780721854` 유지하며 uptime 상승. 둘 다 health ok.
  - tmux windows는 `0 grove-master`, `1 web`, `2 slack`, `3 advisor`, `4 jester` 그대로 유지됐다.

- 2026-06-06 14:50 KST Slack queue timeout retry 보강:
  - Slack queue worker의 `grove ask`가 120초 timeout(`subprocess.TimeoutExpired` 또는 `timed out after`)을 내도 failed/fallback으로 종료하지 않는다. master가 긴 turn 중인 정상 상황으로 보고 pending으로 되돌린 뒤 retry한다.
  - 첫 timeout defer 시 같은 Slack thread에 `아직 <node> 응답을 기다리고 있어 대기열에서 계속 재시도합니다. 완료되면 이 스레드에 답변합니다.` 안내를 1회 남긴다. 이후 답변이 오면 원래 thread에 결과를 게시하고 queue item은 done 처리한다.
  - 검증: `uv run --group dev pytest tests/test_slack.py -q -k 'chat_routing'` 13 passed, bridge full `ruff/mypy/pytest` 457 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 457).

- 2026-06-06 14:46 KST Slack queue 비동기 분리 보강:
  - `eba552d`의 durable queue 방향은 유지하되, enqueue 직후 event handler가 `poll_node_chat_queue()`를 동기 호출하던 약점을 제거했다. 이제 Slack inbound handler는 queue 저장 + thread ack만 하고 즉시 반환한다.
  - queue drain은 별도 daemon worker thread(`grove-slack-node-chat-queue`)가 담당한다. 따라서 master 호출이 길어져도 Slack socket event handler와 main heartbeat loop가 같은 `ask` 호출에 묶이지 않는다.
  - input guard busy가 반복되면 메시지는 계속 pending으로 유지하며 재시도한다. 6번째 busy defer 시 같은 Slack thread에 `아직 <node> 입력창에 작성 중인 내용이 있어 대기열에서 기다리고 있습니다. 메시지가 섞이지 않도록 계속 재시도합니다.` 안내를 1회 남긴다. silent drop하지 않는다.
  - 검증: `uv run --group dev pytest tests/test_slack.py -q -k 'chat_routing'` 12 passed, bridge full `ruff/mypy/pytest` 456 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 456).

- 2026-06-06 14:40 KST Slack thread queue 안정화:
  - chat-master 노드 추가는 보류했다. 사용자/ advisor와 검토한 결론은 "새 노드로 Slack을 중계"가 아니라 Slack connector 내부 durable queue로 `blocking ask grove-master --timeout 120s` 경로를 decouple하는 것이다.
  - `slack_chat_queue` SQLite table을 추가했다. Slack routed chat은 `team/channel/thread_ts/message_ts` 단위로 queue에 저장하고, thread ack는 즉시 `전달 대기열에 넣었습니다. 완료되면 이 스레드에 답변합니다.`라고 응답한다.
  - connector dispatcher는 queue를 단일 lock으로 순차 처리한다. `target pane has unsent prompt input` 가드에 걸리면 메시지를 drop/fallback하지 않고 pending으로 되돌려 다음 poll에서 재시도한다. 다른 transport 실패는 기존 fallback 응답을 thread에 남기고 failed로 종료한다.
  - thread 구분은 Slack `team_id`, `channel_id`, `thread_ts`, `message_ts`와 conversation id `slack:<team>:<channel>:<thread_ts>`로 유지한다. thread마다 노드를 만들지 않는다.
  - 검증: `uv run --group dev pytest tests/test_slack.py -q -k 'chat_routing'` 11 passed, bridge full `ruff/mypy/pytest` 455 passed, `pnpm check` green(TS/Vitest 305, bridge pytest 455).

- 2026-06-06 14:32 KST Slack loop 운영 안정화:
  - `~/.grove/dev10/run-slack-loop.sh`는 더 이상 `uv run ... | tee` 파이프라인으로 Slack child를 실행하지 않는다. 기존 구조에서는 `slack-runtime.json`의 child pid에 TERM을 보내면 loop/pane이 같이 사라져 `dev10:2` window를 수동 복구해야 했다.
  - 현재 runtime script는 bash `set -uo pipefail` + `uv run ... >> "$HOME/.grove/dev10/slack.log" 2>&1` 직접 리다이렉트 구조다. child exit status를 직접 받고 loop shell은 살아남는다.
  - 적용 검증: 새 loop를 `dev10:2`에 적용한 뒤 Slack child TERM smoke에서 pid `60549 -> 60697`로 재시작했고, `dev10:2` window/pane은 유지됐다. 현재 Slack runtime pid는 `60697`, `socket_connected=true`, heartbeat fresh다.
  - 이 변경은 repo-tracked product code가 아니라 live 운영 스크립트 수정이다. main web `8765`, remote web `5173`, org node registry에는 영향을 주지 않았다.

- 2026-06-06 14:27 KST 최신 안정화 상태:
  - 이 항목을 포함하는 최신 product-code HEAD는 `d516175 fix: acknowledge slack chat and guard pane input` 이후 Slack 입력충돌 실패 사유 copy 패치를 적용한다.
  - Slack routed chat은 `route_chat_to_node=True` 경로에서 `grove-master` 호출 전에 같은 thread에 1회 working ack를 즉시 남긴다. 긴 turn 동안 Slack 사용자가 무응답으로 느끼는 문제를 줄이기 위한 안정화이며, ack post 실패는 실제 처리 실패로 만들지 않는다.
  - `grove send`/`grove ask`의 `submitMessage`는 대상 tmux pane을 `capture-pane -e`로 확인하고, 실제 입력줄에 사용자가 작성한 텍스트가 있으면 주입을 거부한다. Codex `› ...`/Claude `❯ ...` 자동 추천 ghost는 dim ANSI 스타일로 구분해 실제 입력으로 보지 않는다. 목적은 사람 입력/CLI 자동 초안/노드 메시지가 섞여 같이 제출되는 회귀를 막는 것이다.
  - Slack routed chat이 이 입력충돌 가드에 걸리면 일반 transport fallback 대신 같은 thread에 `지금 <node> 입력창에 작성 중인 내용이 있어 메시지를 섞지 않도록 전송하지 않았습니다. 입력창을 비운 뒤 다시 보내 주세요.`라고 정확한 실패 사유를 반환한다. 자동 재시도는 약속하지 않고, 실제 처리처럼 메시지를 드롭하고 사용자 재전송을 요청한다. 다른 transport 실패는 기존 fallback 문구를 유지한다.
  - 2026-06-06 14:20 KST 기준 조직 baseline은 5노드다: `0 grove-master`, `1 web`, `2 slack`, `3 advisor`, `4 jester`. `jester`는 social/passive 노드이며 요청받을 때만 짧은 농담을 하고 파일/org/list item을 건드리지 않는다.
  - Slack은 새 코드 반영 중 `dev10:2.0` pane이 사라져 같은 index로 즉시 복구했다. 현재 Slack runtime pid는 `51917`, `socket_connected=true`, heartbeat fresh다.
  - main web `8765`와 보조 원격 web `5173`은 재시작하지 않았고 health ok다. 2026-06-06 14:20 KST 기준 `8765 started_at=1780721628`, `5173 started_at=1780721854`.
  - 검증: `pnpm build`, `uv run --group dev pytest tests/test_slack.py -q -k 'chat_routing'` 11 passed, `pnpm check` green(TS/Vitest 56 files 305 tests, bridge pytest 455), Slack pane 복구 후 `grove org --json` 5노드 전부 `pane_exists=true`, `tmux` windows `0/1/2/3/4`.

- 2026-06-06 13:57 KST 최신 live 운영(아래 13:14 기록보다 우선):
  - 최신 product-code HEAD는 `5d01154 fix: pin web tmux commands to session socket`이다. bridge tmux subprocess와 node connect payload가 pane/session에서 `tmux -L <session>`을 유도하므로 tmux 밖 launchd/SSH/headless 컨텍스트에서도 default socket이 아니라 `dev10` named socket을 결정적으로 본다.
  - main web은 `dev10:1.0`에서 `/Users/chopin/.grove/dev10/run-web-loop.sh`가 관리하며 `0.0.0.0:8765`로 실행 중이다. 2026-06-06 13:57 KST 기준 child pid `34459`, `started_at=1780721628`, health ok.
  - 원격 실시간 UI용 보조 web은 tmux 조직 pane에 올리지 않는다. `~/Library/LaunchAgents/local.grove.client-web-5173.plist`가 tmux 밖 LaunchAgent로 `0.0.0.0:5173`을 실행한다. 2026-06-06 13:57 KST 기준 pid `36987`, `started_at=1780721854`, health ok. plist는 `PATH=/opt/homebrew/bin:/opt/homebrew/sbin:/usr/bin:/bin:/usr/sbin:/sbin`, `GROVE_HOME=/Users/chopin/.grove`, `--session dev10`, `--unsafe-bind`, `--enable-node-input`, `--enable-intake`, `--allow-host 100.100.90.87,192.168.1.186`를 명시한다. `$TMUX` 하드코딩은 제거했으며, liveness는 코드의 `tmux -L dev10` 경로로 검증한다.
  - 운영자 접속 URL은 `http://100.100.90.87:5173/`이다. 자동 local-token bootstrap이 HTML에 주입되고, `/api/status`는 `running=4`, `/api/org`는 `default_assignee=grove-master`, 4개 노드 모두 `pane_exists=true`를 반환한다. `8765`와 `5173`의 `/api/nodes/grove-master/connect`는 둘 다 `ssh 100.100.90.87 'tmux -L dev10 select-pane -t dev10:0.0 && tmux -L dev10 attach -t dev10'`를 반환한다.
  - 현재 tmux 조직은 `dev10` 단일 세션의 windows `0 grove-master`, `1 web`, `2 slack`, `3 advisor`만 사용한다. 5173 보조 서버는 이 조직도에 노드/window로 추가하지 않는다.
  - Slack은 재시작하지 않았고 runtime pid `59036`, `socket_connected=true`, heartbeat fresh를 유지한다.
  - 검증: `pnpm check` green(TS/Vitest 56 files 302 tests, bridge pytest 454), `node web/e2e/live.mjs` 94/94 green(`LIVE_E2E_CLEANUP matched=2`), DB residue `tasks=0`, `comments=0`, `p2-test` board row 0, `~/.grove/p2-test/registry.json` absent, git tree clean.

- 2026-06-06 13:14 KST 기준 최신 live 운영:
  - 현재 노드는 `grove-master`이며 `dev10:0.0`, cwd `/Users/chopin/dev/grove`에서 실행된다.
  - 단일 tmux 세션 `dev10`만 사용한다. panes: `dev10:0.0 grove-master`, `dev10:1.0 web`, `dev10:2.0 slack`, `dev10:3.0 advisor`.
  - web은 `dev10:1.0`에서 `/Users/chopin/.grove/dev10/run-web-loop.sh`로 실행한다. 명령은 `0.0.0.0:8765`, `--unsafe-bind`, `--enable-node-input`, `--enable-intake`, `--allow-host 100.100.90.87,192.168.1.186`를 포함한다. 2026-06-06 13:14 KST 기준 `started_at=1780719114`, child pid `7417`이며 N6/N10/N2 UI 변경, `32ffdf9` board-listing fix, `d5cdfea` planner limitation item-copy fix, `42e6fb7` ledger quota item/work wording fix, `e88968e` web metadata cockpit-model fix, `8ca8992` handoff accept item wording fix, `899f746` live cockpit naming 정합, `631f3f4`/`92c6046` frontend item-copy 정합 static build가 live에 반영됐다.
  - 원격 접속 URL은 tailnet `http://100.100.90.87:8765`, LAN `http://192.168.1.186:8765`이다. remote terminal은 tailnet URL에서 실제 Chrome smoke로 `.dr-conn is-live`, xterm 렌더, 기본 선택 `grove-master`/`dev10:0.0`을 확인했다. `~/.grove/dev10/web.json`도 `allowed_hosts`와 `remote_urls`를 노출한다.
  - Slack은 `dev10:2.0`에서 `/Users/chopin/.grove/dev10/run-slack-loop.sh`로 실행한다. `/api/slack/config/status`는 `socket_connected`, `~/.grove/dev10/slack-runtime.json` heartbeat가 갱신된다. 2026-06-06 13:14 KST 기준 Slack pane은 `dev10:2.0`, runtime pid는 `59036`, heartbeat fresh이다. `a9f8478` Slack control item copy cleanup 반영을 위해 같은 pane을 respawn했으며 socket 재연결을 확인했다.
  - advisor는 `dev10:3.0`의 Claude 노드이며 약 5분마다 `grove-master`를 점검한다. 사용자가 명시 중단하기 전까지 루프를 멈추지 않는다.
  - `/api/projects`는 `dev10` 하나만 반환해야 한다. `/api/boards`는 프로젝트 헤더가 없어도 현재 active project board만 반환해야 하며, 과거 `p2-test` 같은 stale board가 섞이면 회귀다. `/api/org`의 `default_assignee`와 `master_org.project_master.name`은 `grove-master`여야 하며 advisor가 default가 되면 회귀다.
  - 최신 live API contract fix는 `8ca8992 fix: describe handoff accept output as items`이다. `/api/handoff/accept` limitations가 `local unassigned ready task` 대신 `human-facing local item`을 반환하도록 정렬했고, `web/verify.mjs`가 web API/HandoffPanel source의 stale `local task` copy 재유입을 막는다. RED→GREEN targeted bridge pytest, `web npm run verify`, `node web/e2e/api.mjs` 694/694, `pnpm check` green(TS/Vitest 56 files 300 tests, bridge pytest 454), web-only graceful 재배포 후 live `node web/e2e/live.mjs` 84/84 passed(`LIVE_E2E_CLEANUP matched=2`). 실행 후 DB `tasks=0`, `comments=0`, `p2-test` board/registry residue 0, tmux session `dev10` only, panes `dev10:0/1/2/3`, Slack pid `59036` heartbeat fresh 확인.
  - 직전 live API contract fix는 `42e6fb7 fix: align ledger quota copy with work wording`이다. `/api/ledger` limitations/warnings가 `running tasks` 대신 `running work`/`running work is not stopped`를 반환하도록 정렬했고, `web/verify.mjs`가 bridge source와 `web/e2e/api.mjs`까지 스캔해 backend payload stale wording 누락을 막는다. RED→GREEN targeted bridge pytest, `web npm run verify`, `node web/e2e/api.mjs` 693/693, `pnpm check` green(TS/Vitest 56 files 298 tests, bridge pytest 453), web-only graceful 재배포 후 live `node web/e2e/live.mjs` 84/84 passed(`LIVE_E2E_CLEANUP matched=2`). 실행 후 DB `tasks=0`, `comments=0`, `p2-test` board/registry residue 0, tmux session `dev10` only, panes `dev10:0/1/2/3`, Slack pid `59036` heartbeat fresh 확인.
  - 이전 live API contract fix는 `32ffdf9 fix: hide legacy dev-room alias from board listing`이다. `/api/boards`가 `X-Grove-Project: dev10` 헤더에서도 active project board `dev10`만 반환하도록 정렬했다. legacy `/api/boards/dev-room/*` 직접 접근은 dev10 소유 compatibility endpoint로 유지하되 목록에는 노출하지 않는다. RED(`['dev-room','dev10']`)→GREEN targeted pytest, `pnpm check` green(TS/Vitest 56 files 292 tests, bridge pytest 453), web graceful 재배포 후 live smoke: no-header `/api/boards=[dev10]`, dev10-header `/api/boards=[dev10]`, `/api/boards/dev-room/tasks` 200 `[]`, `/api/status running=4`, `/api/projects=[dev10]`, `/api/org` 4 nodes `pane_exists:true`, Slack pid `59036` heartbeat fresh.
  - `~/.grove/boards/board.db`의 stale `p2-test` board/task 찌꺼기는 삭제했다. 삭제 전 백업은 `~/.grove/boards/board.db.bak.pre-p2-live-cleanup-1780711497`이다. `b19817a` 이후 live e2e는 자기 RUN_ID의 `p2-live-*` tasks/comments/events와 비어 있는 `p2-test` board row까지 finally에서 정리한다. 이후 DB 전체 `tasks=0`, `comments=0`, `p2-test` board/registry residue 0, live `/api/boards=[dev10]`, `/api/inbox.total=0`을 확인했다.
  - 최신 UI 기능 product-code 커밋은 `99ce1f8 feat: add board group bulk assignment`이다. 이후 safety/docs/test 정합 커밋이 HEAD에 추가될 수 있으므로 `git log --oneline -5`로 현재 HEAD와 UI 기능 HEAD를 구분한다.
  - 최신 web cockpit naming/current-doc 정합 커밋은 `899f746 fix: align web cockpit naming`이다. `web/index.html` browser title과 `web/src/i18n.tsx` brand/onboarding copy를 `dev room` 모델이 아니라 live cockpit 모델로 정렬했고, `bridge/src/grove_bridge/web_app.py` startup/bind-refusal/help copy도 `Grove cockpit`으로 맞췄다. `src/current-docs.test.ts`와 `bridge/tests/test_web_app.py`가 해당 stale wording 재유입을 막는다. 검증: RED→GREEN current-docs/bridge targeted/web verify, `pnpm check` green(TS/Vitest 56 files 302 tests, bridge pytest 454), web-only graceful 재배포 후 live `node web/e2e/live.mjs` 84/84 passed(`LIVE_E2E_CLEANUP matched=2`). 실행 후 DB `tasks=0`, `comments=0`, `p2-test` residue 0, tmux `dev10` only, Slack pid `59036` heartbeat fresh 확인.
  - 직전 web metadata/current-doc 정합 커밋은 `e88968e fix: align web metadata with cockpit model`이다. FastAPI app title을 `grove cockpit`으로 정렬하고, `web/package.json`, `web/build.mjs`, `web/src/styles.css`, `web/mock/index.html`, `web/mock/harness.ts`의 `Grove Dev Room`/`Dev Room SPA`/`dev-room SPA` stale wording을 `Grove web cockpit` 모델로 정리했다. `bridge/tests/test_web_app.py`, `src/current-docs.test.ts`, `web/verify.mjs`가 재유입을 막는다. 검증: RED→GREEN targeted bridge/current-docs/web verify, `pnpm check` green(TS/Vitest 56 files 300 tests, bridge pytest 454), web-only graceful 재배포 후 live `node web/e2e/live.mjs` 84/84 passed(`LIVE_E2E_CLEANUP matched=2`). 실행 후 DB `tasks=0`, `comments=0`, `p2-test` residue 0, tmux `dev10` only, Slack pid `59036` heartbeat fresh 확인.
  - 최신 web README/current-doc 정합 커밋은 `af52410 docs: align web README with cockpit model`이다. `web/README.md`의 `Grove Dev Room`/`kanban board`/`task detail`/`dev-room SPA` current wording을 `Grove web cockpit`/`human-facing items`/item drawer/terminal mirror 모델로 정렬했고, `src/current-docs.test.ts`가 재유입을 막는다. 검증: RED→GREEN, `pnpm vitest run src/current-docs.test.ts` 8/8, `pnpm check` green(TS/Vitest 56 files 299 tests, bridge pytest 453).
  - 직전 current-model 정합 커밋은 `0191223 docs: mark legacy team auth design historical`이다. top-level `docs/DESIGN_team_auth.md`는 historical v1.2 design으로 명확히 표시하고 현행 live auth 문서는 `docs/design/AUTH_AND_PROJECTS.md`라고 안내한다. `src/current-docs.test.ts`가 이 historical marker를 가드한다. 검증: `pnpm vitest run src/current-docs.test.ts`, `pnpm check` green(TS/Vitest 56 files 292 tests, bridge pytest 453).
  - 최신 README/current-doc 정합 커밋은 `e84a0a6 docs: align README with cockpit model`이다. README의 `dev-room web SPA`/`Web dev-room SPA`/`dev-room list access` current wording을 `web cockpit`/compatibility list access로 정렬하고, 프로젝트 모델도 `one project, one tmux session`이 아니라 project identity + explicit workspace/registry + project-scoped list store + shared host tmux session 가능 모델로 설명한다. `src/current-docs.test.ts`가 이 stale phrase 재유입을 막는다. 검증: RED→GREEN, `pnpm vitest run src/current-docs.test.ts`, `pnpm check` green(TS/Vitest 56 files 298 tests, bridge pytest 453).
  - 직전 current-model 정합 커밋은 `78d9f48 docs: align auth lifecycle design with live model`이다. `docs/design/AUTH_AND_PROJECTS.md`는 Phase-1 scaffold 문서가 아니라 live team-auth/share/join/CSRF/project lifecycle 모델을 설명한다. `src/current-docs.test.ts`가 `AUTH_AND_PROJECTS.md`의 `Status: current v2 live auth and project lifecycle model`, team-auth live bridge/web UI, shared access one-time join code 문구를 가드하고 `Phase 1 is design...`/`no web_app.py route registration`/`no app.tsx...`/phase-1 bridge module 회귀를 막는다. 검증: `pnpm vitest run src/current-docs.test.ts`, `pnpm check` green(TS/Vitest 56 files 291 tests, bridge pytest 453).
  - 직전 current-model 정합 커밋은 `64f8b02 docs: align master node design with live model`이다. `docs/design/MASTER_NODE.md`는 Phase-1 adapter-only/read-only proposal 문서가 아니라 live `grove-master` node-routed v2 모델을 설명한다. `src/current-docs.test.ts`가 `MASTER_NODE.md`의 `Status: current v2 live model`, Slack/web live route, human-facing list records 문구를 가드하고 `## Phase-1 Scope`/`no web_app.py route registration`/`Only read-only answers...` 회귀를 막는다. 검증: `pnpm vitest run src/current-docs.test.ts src/resilience-scripts.test.ts`, `pnpm check` green(TS/Vitest 56 files 290 tests, bridge pytest 453).
  - 최신 safety 스크립트 정합 커밋은 `508c2a6 fix: guard restore against live web port`이다. `scripts/restore.sh`가 현행 live web port `8765`와 legacy `9131`을 모두 거부하고, `docs/design/RESILIENCE.md`/`docker/README.md`도 live guard를 `8765` 중심으로 정렬했다. 검증: `pnpm vitest run src/resilience-scripts.test.ts`, `pnpm check` green(TS/Vitest 55 files 289 tests, bridge pytest 453).
  - 최신 UI/test-harness 정합 커밋은 `99ce1f8 feat: add board group bulk assignment`이다. 보드 새 항목 폼의 담당자 select가 현재 org group을 `group:<name>` target으로 노출하고, 그룹 선택 시 해당 그룹의 현재 assignee 후보 멤버마다 같은 사람용 항목을 생성한다. N10은 group filter + group bulk assignment까지 `covered`로 갱신됐다. 이전 `9731df4`는 조직도 reparent 키보드 대안(N8), `c268635`는 Item drawer Slack thread entrypoint(N2)를 추가했다. 검증: N10 RED(그룹 선택 후 3개 POST 미생성)→GREEN, `web npm run check`, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453 passed). web static build가 live에 반영됐고 web/slack 프로세스 재시작은 하지 않았다. 실행 후 tmux session은 `dev10` 하나, panes `dev10:0/1/2/3`, registry `~/.grove/dev10/registry.json` 하나만 남는 것을 확인했다.
  - 최신 UI/API wording 정합 커밋은 `42e6fb7 fix: align ledger quota copy with work wording`이다. `/api/ledger` backend payload와 API e2e가 `running tasks` 대신 `running work is not stopped` 모델을 사용한다. 직전 `d5cdfea`는 `/api/plan` read-only limitation과 web mock의 `No task is claimed...`를 `No item is claimed...` 모델로 정렬했고, `631f3f4`는 frontend ledger/soft quota 경고와 mock limitation의 `running tasks not killed`류를 `running work not stopped` 모델로 정렬했으며, `92c6046`은 Item drawer dialog 접근성 label의 `task detail`을 `item detail`로 정렬했다. 이전 `e45c882`/`bcd5e76`/`7d53845`/`ebce8ff`/`91a08d1`의 item/list wording guard도 유지된다. 검증: RED→GREEN cycles, `web npm run verify`, `node web/e2e/api.mjs` 693/693, `pnpm check` green(TS/Vitest 56 files 298 tests, bridge pytest 453), web-only graceful redeploy 후 live `node web/e2e/live.mjs` 84/84 passed(`LIVE_E2E_CLEANUP matched=2`). 실행 후 DB `tasks=0`, `comments=0`, `p2-test` board/registry residue 0, tmux session `dev10` only, panes `dev10:0/1/2/3`, Slack pid `59036` heartbeat fresh 확인.
  - 이전 검증: live headless/dev10 노드가 registry `connect_host`를 갖지 않아 node connect payload가 로컬 `tmux attach`로만 떨어지던 약한 지점을 고쳤다. node-level host를 우선하고, 없으면 web config의 trusted `allowed_hosts` 첫 non-loopback host로 SSH tmux attach를 만든다. RED(local attach)→GREEN, `uv run --group dev pytest tests/test_web_app.py -q -k 'node_connect_info'` 2 passed, `uv run --group dev pytest tests/test_web_app.py -q` 211 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453 passed). web만 graceful 재시작해 `started_at=1780701550`으로 새 코드 반영, Slack은 재시작하지 않고 pid `59036` 유지. live `/api/nodes/grove-master/connect`는 `mode=ssh_tmux_attach`, label `SSH tmux attach (100.100.90.87)`, `ssh_attach` 존재를 반환한다. `node web/e2e/tier1/runner.mjs --live` 7/7 passed, 현재 live는 web health 200, `/api/status running=4`, all 4 nodes `pane_exists:true`, exact panes `dev10:0/1/2/3`, registry `dev10` only, Slack `socket_connected` heartbeat fresh이다.
  - 이전 검증: strict API 404 hardening 이후 `MasterChat` 초기 로드가 `GET /api/master/chat` 404를 브라우저 console error로 남기던 회귀를 고쳤다. backend GET history endpoint는 인증/프로젝트 resolve 후 `{"messages":[]}`를 반환한다. RED(404)→GREEN, `uv run --group dev pytest tests/test_web_app.py -q` 210 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 452 passed). web만 graceful 재시작해 `started_at=1780700385`로 새 코드 반영, Slack은 재시작하지 않고 pid `59036` 유지. live `GET /api/master/chat`는 `{"messages":[]}`, unknown `/api/*`는 여전히 JSON 404다. `node web/e2e/live.mjs` full live journey 83/83 passed(no unexpected 404/console error/5xx), p2-test/qae2e registry/tmux cleanup 확인.
  - 관련 검증: live e2e 기본 포트와 registry/doc stub의 stale `9131` 참조를 현행 `8765`/generic live cockpit으로 정리했고, `web/verify.mjs`가 9131 재유입을 막는다. `node web/e2e/tier1/runner.mjs --live` env 없이 7/7, `web npm run check`, `pnpm check` green.
  - 관련 검증: pane-liveness hardening 이후 web API e2e fixture drift를 수정해 disposable `qae2e_*` tmux sessions/panes를 직접 만들고 teardown에서 제거한다. `npm run e2e` 693/693, `npm run check`, `pnpm check` green; e2e 종료 후 `qae2e_*` tmux session/pane 잔존 0 확인.
- 현재 노드는 `grove-master`이며 `dev10:0.0`, cwd `/Users/chopin/dev/grove`에서 실행된다.
- 앞으로 기본 운영은 `dev10` tmux 하나를 쓴다. 현재 서비스 창은 `dev10:1.0 web`, `dev10:2.0 slack`이다.
- 프로젝트 ID와 host tmux 세션은 분리됐다. web 프로젝트 생성은 `grove new-project --tmux-session dev10`로 새 프로젝트 registry를 만들고 pane은 `dev10`에 둔다.
- 사용자가 Slack 재가동을 명시 승인했다. Slack은 `grove-master`와 직접 대화하도록 켜져 있으며, 긴 응답은 Slack thread 안에서 chunking한다.
- web은 원격 접속을 위해 `0.0.0.0:8765`에 떠 있어야 한다. 현재 tailnet URL은 `http://100.100.90.87:8765`, LAN URL은 `http://192.168.1.186:8765`이다.
- 사람용 목록 항목은 노드 간 통신 프로토콜이 아니다. 사람 TODO, 사람 피드백, ask-human/판단 대기 기록으로만 취급한다.
- 2026-06-06 13:14 KST 기준 live board DB의 `tasks=0`, `comments=0`, `p2-test` fixture board row 잔존 0, 모든 남은 board slug의 `task_count=0`, `/api/boards == [dev10]`, `/api/inbox total=0`이다. 웹 보드는 "피드백 및 할 일" / "사람 판단 필요" 두 목록만 기본 노출한다.
- 노드 간 통신은 직접 대화, tmux capture/send, CLI ask/send 등 각 노드 판단에 맡긴다.
- 조직도 변경은 사람 소유다. GUI/API/CLI에서 명시 operator 경로(`--operator`, `--operator-override`)로만 수행한다.
- 모든 노드는 조직도, 역할, tmux pane, cwd를 볼 수 있어야 한다. `grove org --json`과 `/api/org`가 이 필드를 노출한다. tmux pane liveness는 `list-panes`의 canonical `session:window.pane` 목록과 완전 일치로 검증한다. missing pane은 CLI에서 `pane_exists:false/status:pane-missing`, web API에서 `status:dead/unavailable_reason:"tmux pane missing"`으로 보여야 한다.
- 2026-06-05 23:12 KST 기준 `/opt/homebrew/bin/grove -> /Users/chopin/dev/grove/dist/cli.js` symlink를 생성했다. 새 셸에서 `grove org --json`이 바로 동작한다.
- web 기본 sidebar는 `목록`, `Team`, `터미널`, `Slack integration`, `Inbox`, `Audit`, `Setup`만 노출한다. `Connect`/구형 execution/cost/ledger/aggregation/handoff/routing/chain 표면은 기본 UI에서 제거됐고, join 딥링크(`?join=`) 호환은 유지한다.
- 터미널 탭은 선택 pane이 없으면 첫 viewable pane에 자동 연결한다. 2026-06-05 23:19 KST 장애 원인=첫 방문 onboarding overlay가 실제 클릭을 가로막음. 수정=onboarding은 수동 튜토리얼 버튼으로만 열림. live 검증: 실제 Chrome 클릭으로 `터미널` 탭 진입, `/api/ws-ticket` terminal ticket 200, `/ws/terminal` frame 수신, xterm text 렌더 확인.
- `build_assistant_facts`는 현재 `dev10` registry 기준 live nodes를 반환한다. facts는 참고 컨텍스트이며 응답의 유일한 상태 소스가 아니다.
- Slack/web master 응답 프롬프트와 사용자-facing 차단 사유에서 내부 구현어가 새지 않도록 정리했다. facts/decision JSON은 이벤트 컨텍스트일 뿐이며 자연 대화와 직접 런타임 확인을 막지 않는다. 2026-06-05 23:35 KST live 반영됨(PID web 51497, slack 50643 확인).
- 검증: `npm run build`, `npm run verify`(core UI), `GROVE_VERIFY_FULL=1 npm run verify`(현재 core로 수렴), `pnpm check` green. `GROVE_VERIFY_FULL=1 GROVE_VERIFY_LEGACY_FULL=1 npm run verify`는 과거 전체 패널 nav를 되살리지 않고, 은퇴한 구형 표면이 기본 sidebar/command palette에 없고 `?join=` 딥링크만 유지되는지 검증한다.

## 0. 과거 사고 기록 (현재 운영 규칙 아님)

- 과거에는 Slack 봇이 사용자 승인 전 OFF였다. 2026-06-05에 사용자가 재가동을 명시 승인했고, 현재는 `grove-master` 직통 라우팅으로 실행 중이다.
- 라이브 dev10 노드 비파괴(despawn/recreate 금지, 문맥 가치 높음). 게시 history 비재작성. web 단일 인스턴스(`~/.grove/redeploy-web.sh`).
- watchdog/executor OFF 유지(멀티리뷰+사용자 승인 전 실가동 금지).
- 과거 handoff에는 lead가 통합만 맡는 운영 제한이 있었다. 현재 사용자는 필요하면 master가 직접 작업해도 된다고 정정했다.

## 1. 현재 product-code = 899f746 (HEAD는 test/docs-only handoff 커밋일 수 있음)

최근 완료된 안정화:

- `d9fed16 test: guard live e2e item drawer wording` / `ccb4c86 test: guard frontend item wording` / `ab9d602 docs: align event-tail item wording`: current web source/mock/e2e/verify에서 `Task drawer`/`Board event-tail`/`solo task` stale wording을 제거하고, `web/verify.mjs`가 frontend source + CSS + mock + live e2e + verify source까지 item/list wording을 정적 가드한다. RED(새 guard가 mock `board event-tail` 잔여를 잡아 `npm run verify` fail)→GREEN 확인. `web npm run check`, `pnpm check` green(TS/Vitest 56 files 294 tests, bridge pytest 453). frontend verify/docs-only라 live web/slack 재시작 불필요. live 상태: web `started_at=1780711102` uptime climbing, Slack pid `59036` heartbeat fresh, `/api/status running=4`, `/api/boards=[dev10]`, DB tasks/comments/p2-test row 0.
- `91f856d test: guard mock item title wording`: `web/verify.mjs`의 mock item-title guard를 정적 source/bundle 검사까지 확장하고, full verify project-switch path가 `solo task`가 아니라 `solo item`을 기다리도록 정렬했다. exact grep(`Task drawer|Board event-tail|solo task`)은 handoff 기록/guard 설명 외 web source에서 0건이며, `web npm run check`, `pnpm check` green(TS/Vitest 56 files 294 tests, bridge pytest 453). frontend verify-only라 live web/slack 재시작 불필요.
- `5c3c7dd test: align mock item titles`: mock 기본 목록 카드 제목에서 `Task drawer`/`Board event-tail`/`solo task` stale copy를 `Item drawer`/`List event-tail`/`solo item`으로 정렬했다. `web/verify.mjs`가 visible card titles에 `Task drawer|Board event-tail` 재유입을 막는 `mockItemTitlesOk` 가드를 추가했다. RED(`mockItemTitlesOk:false`)→GREEN, `web npm run check`, `pnpm check` green(TS/Vitest 56 files 294 tests, bridge pytest 453). frontend mock/verify-only라 live web/slack 재시작 불필요.
- `a0e67ec test: hide ghost boards in web mock`: web mock이 `/api/boards` 목록에서는 active board 하나만 노출하면서도 직접 `/api/boards/infra/tasks`를 200으로 열어주던 ghost-board drift를 제거했다. mock resolver는 `default`/`main` alias와 active project board만 허용하고 unknown/hidden board는 JSON 404를 반환한다. `web/verify.mjs`에 `mockGhostBoardHidden` RED→GREEN 가드를 추가했다. 검증: `web npm run check`, `pnpm check` green(TS/Vitest 56 files 294 tests, bridge pytest 453). frontend mock/verify-only라 live web/slack 재시작 불필요.
- `c6d2c9f test: clean live e2e board fixtures` / `b19817a test: remove live e2e board row residue`: live e2e가 `p2-test`에 생성하는 `p2-live-*` 사람용 항목을 RUN_ID 기준으로 finally에서 직접 정리하도록 했다. SQLite cleanup은 관련 decision locks, deps, notify subs, slack threads, comments, runs, events, tasks와 비어 있는 `p2-test` board row를 같은 transaction에서 제거하고 실패 시 e2e를 실패시킨다. `web/verify.mjs`가 `cleanupLiveE2eBoardFixtures(RUN_ID)`와 empty board row cleanup 재유입을 정적 가드하며, N2 drawer Slack thread 검증은 async fetch 완료를 기다리도록 race를 제거했다. 기존 live DB 잔여 `p2-test` 22 tasks/11 comments/173 events는 백업 `~/.grove/boards/board.db.bak.pre-p2-live-cleanup-1780711497` 생성 후 정리했다. live `node web/e2e/live.mjs` 84/84 통과(`LIVE_E2E_CLEANUP matched=2`) 후 `tasks=0`, `comments=0`, `p2-test` board/registry residue 0, `/api/boards=[dev10]`, `/api/inbox total=0`, `/api/projects=[dev10]` 확인. 검증: `web npm run verify`, `web npm run check`, live e2e 84/84, `pnpm check` green(TS/Vitest 56 files 292 tests, bridge pytest 453). e2e/verify-only라 web/slack 재시작 불필요.
- `32ffdf9 fix: hide legacy dev-room alias from board listing`: `/api/boards`가 `X-Grove-Project: dev10` 요청에서 legacy `dev-room` alias까지 목록에 섞어 반환하던 누수를 제거했다. active project listing은 `dev10`만 반환하고, 직접 `/api/boards/dev-room/tasks` 접근 compatibility는 dev10 owner에서 유지한다. RED(`[dev-room, dev10]`)→GREEN(`[dev10]`), targeted bridge tests 4 passed, `pnpm check` green(TS/Vitest 56 files 292 tests, bridge pytest 453). backend 변경이라 web-only graceful redeploy를 했고 live `started_at=1780711102`, Slack pid `59036` 유지, `/api/boards=[dev10]`, direct dev-room tasks 200, `/api/status running=4`, `/api/projects=[dev10]`, `/api/org default_assignee=grove-master`, panes `dev10:0/1/2/3` 확인.
- `99ce1f8 feat: add board group bulk assignment`: 보드 새 항목 폼의 담당자 select에 현재 org group target을 추가하고, `group:<name>` 선택 시 해당 그룹의 현재 assignee 후보 멤버별로 같은 사람용 항목을 생성한다. 기존 단일 노드 선택 경로는 유지된다. `web/verify.mjs` core N10에 `group:build` 선택 후 `backend/docs/frontend` 3개 POST 회귀 가드를 추가하고, `docs/USER_JOURNEYS.md` N10을 `covered`로 갱신했다. 검증: RED(그룹 선택 후 3개 POST 미생성)→GREEN, `web npm run check`, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453). frontend-only라 web static build 반영으로 live에 적용되고 web/slack 프로세스 재시작은 불필요하다.
- `9731df4 feat: add org keyboard parent control`: 조직도 노드 hover action에 부모 선택 select를 추가해 drag-only reparent의 키보드 대안을 제공한다. 후보는 자기 자신/현재 부모/descendant를 제외하고, 기존 연결선/노드 detach 경로와 함께 N8 접근성 여정을 닫는다. `web/verify.mjs` core `#N8 org accessibility snapshot`에 `keyboardParentSelects` RED→GREEN 가드를 추가하고 `docs/USER_JOURNEYS.md` N8을 `covered`로 갱신했다. 검증: `web npm run build && npm run verify` RED(`keyboardParentSelects:false`)→GREEN, `web npm run check`, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453), live Chrome smoke에서 Team 4 nodes, parent select 3개, aria-label/options present, action overflow 0. web static build가 live에 반영됐고 web/slack 프로세스 재시작은 하지 않았다.
- `c268635 feat: surface slack threads in item drawer`: Item drawer가 `/api/slack/threads?task_id=`를 함께 fetch하고 human-gate/ask-human Slack thread가 있으면 Slack 앱 thread link와 channel/thread/node metadata를 read-only로 표시한다. API 실패는 빈 목록으로 degrade해 drawer 본문/런/댓글 표시를 깨지 않는다. mock harness에 `G-7` Slack thread fixture를 추가하고 `web/verify.mjs` core N2를 RED→GREEN으로 갱신했다. `docs/USER_JOURNEYS.md` N2는 `covered`로 정리했다. 검증: `web npm run check` green(`n2Ok:true`, `boardGroupFilterOk:true`), `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453), web-only graceful redeploy 후 `node web/e2e/live.mjs` 84/84 passed, live health `started_at=1780707710`, Slack pid `59036` fresh, dev10-only tmux/registry 확인.
- `bb60c5e feat: add board group filter`: 보드 사람용 목록에 org group 필터를 추가했다. `BoardView`가 `/api/nodes`에서 받은 node group metadata로 assignee→group을 계산하고, 선택한 group의 assignee 항목만 표시한다. 필터는 표시 전용이며 조직도/배정/항목 상태를 mutate하지 않는다. `web/verify.mjs`에 `#N10 board group filter` RED→GREEN 가드를 추가했다. 당시 그룹 일괄 배정은 후속 작업으로 남겼고, 이후 `99ce1f8`에서 group bulk assignment까지 `covered`로 닫혔다. 검증: `web npm run check` green(`boardGroupFilterOk:true`), `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453), `node web/e2e/live.mjs` 84/84 passed, live Chrome smoke에서 `모든 그룹/master/services/advisory` 옵션 확인. web static build 반영만 필요했고 web/slack 프로세스 재시작은 하지 않았다.
- `119f6a3 test: avoid reentrant live master chat`: full live e2e가 현재 실행 중인 `grove-master` 턴 안에서 `/api/master/chat` POST를 다시 보내 self-call timeout을 만들 수 있던 하네스 회귀를 정리했다. live 여정은 master chat UI surface, `GET /api/master/chat` history endpoint, prompt 입력/XSS probe, send affordance만 확인하고 routed POST는 bridge regressions와 mock verify에 맡긴다. `web/verify.mjs`에 live e2e의 `/api/master/chat` POST 재유입 금지 가드를 추가했다. RED(static guard failed on old POST)→GREEN, `node web/e2e/live.mjs` 84/84 passed(SKIP master chat live POST), `node web/e2e/tier1/runner.mjs --live` 7/7, `web npm run check`, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453), e2e 후 tmux/registry dev10-only cleanup 확인.
- `f21a6bd fix: prefer remote ssh attach for headless nodes`: headless Mac mini 운영에서 registry node에 `connect_host`가 없어도 web config `allowed_hosts`의 첫 trusted non-loopback host로 SSH tmux attach를 제공한다. node-level `connect_host`가 있으면 기존처럼 우선한다. RED(local attach fallback)→GREEN(`test_node_connect_info_uses_allowed_host_for_headless_ssh`), `test_web_app.py` 211 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 453), live web-only graceful restart 후 `started_at=1780701550`, Slack pid `59036` 유지. live `/api/nodes/grove-master/connect`가 `mode=ssh_tmux_attach`, label `SSH tmux attach (100.100.90.87)`, `ssh_attach` 존재를 반환하고 live tier1 7/7 passed.
- `docs: align skeleton role preset creation wording`: skeleton gap 현재 기준의 role preset 주입 문구를 사람이 명시한 GUI/API/CLI 생성 경로로 정렬했다. docs-only, live 재시작 불필요.
- `e222b5a fix: serve empty master chat history`: strict `/api/*` miss JSON 404 이후 frontend `MasterChat`가 mount 때 `GET /api/master/chat`을 호출하며 404/console error를 만들던 문제를 고쳤다. backend GET history endpoint는 인증과 project resolve를 거쳐 `{"messages":[]}`를 반환한다. RED(`GET /api/master/chat` 404)→GREEN, targeted 3 passed, `test_web_app.py` 210 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 452), live web-only graceful restart 후 `started_at=1780700385`, Slack pid `59036` 유지. live `GET /api/master/chat` empty history, unknown `/api/*` JSON 404 유지, `/api/status running=4`, `node web/e2e/live.mjs` 83/83 passed(no unexpected 404/console error/5xx), p2-test/qae2e cleanup 확인.
- `523ad39 fix: prevent api path spa fallback`: API path miss가 SPA catch-all로 내려가 HTML/token bootstrap을 반환하던 문제를 막았다. `/api/*` GET miss는 이제 JSON 404(`api endpoint not found`)로 응답하고, 정상 `POST /api/ws-ticket` terminal ticket 발급은 유지된다. RED(`GET /api/ws-ticket` 200 HTML)→GREEN, `test_web_app.py` 209 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 451), live web-only graceful restart 후 GET miss 404 JSON, POST ticket 30s TTL, `/api/status running=4`, `web npm run check`, live tier1 7/7 확인. Slack은 재시작하지 않았고 pid `59036` 유지.
- `cc80b1c fix: route master chat to visible master node`: 선택 프로젝트가 `p2-test` 같은 임시/다른 프로젝트여도 MASTER chat은 visible registry의 live `grove-master`로 라우팅돼야 한다. 기존 guard가 현재 프로젝트 registry와 `.master`만 검사해 full live e2e에서 `/api/master/chat` 503을 냈다. `_node_routed_target_available`을 visible project registry 전체 + `.master` 스캔으로 정렬하고 regression을 추가했다. RED(503)→GREEN, `test_web_app.py` 208 passed, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 450), live web-only graceful restart 후 `node web/e2e/live.mjs` 83/83 passed, fixture cleanup 확인. Slack은 재시작하지 않았고 pid `59036` 유지.
- `4cd0d19 test: remove stale live port references` / `ba19cfc test: align live e2e default port`: live e2e 기본 URL을 현행 `127.0.0.1:8765`로 정렬하고 stale `9131` 참조를 verify 금지 regex만 남기도록 정리했다. `node web/e2e/tier1/runner.mjs --live` env 없이 7/7, `web npm run check`, `pnpm check` green.
- `4b3dfd5 docs: align skeleton gap spawn wording`: skeleton gap 문서의 과거 "자유spawn" 표현을 현재 v2의 "사람이 명시한 operator-marked 조직 변경" 모델로 정리했다. docs-only, `pnpm check` green.
- `8000a9a test: use real tmux panes in api e2e`: pane-liveness hardening 이후 web API e2e가 fake tmux pane을 가진 worker/lead fixture를 만들던 drift를 수정했다. e2e가 disposable `qae2e_*` tmux sessions/panes를 직접 만들고 teardown에서 제거하므로 node-scoped execution/autopickup/connect/ws-ticket 경로가 product와 같은 liveness 조건에서 검증된다. `npm run e2e` 693/693, `npm run check` green, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 449), e2e 후 `qae2e_*` tmux session/pane 잔존 0 확인.
- `4556041 docs: align claude handoff wording with items`: CLAUDE runtime guide에 남은 `human-facing task` 표현을 `human-facing item`으로 정리했다. 현행 운영 문서 grep(`AGENTS.md`, `CLAUDE.md`, README, USER_GUIDE, USER_JOURNEYS, shared/workstream docs)에서 `human-facing task`/`Board tasks are`/`delegation protocol` 현재모델 잔여 0건을 확인했다. docs-only라 live 재시작 불필요. `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 449).
- `64429a6 fix: describe task command items in help`: 호환 명령명 `grove task`는 유지하되 사용자 help copy를 사람용 item 모델로 정렬했다. `task_id` metavar/`human-facing task`/`updated task`/`previous task status`/`task run id` 노출을 `item_id`/`human-facing item`/`updated item`/`previous item status`/`item run id`로 교체했고, `src/cli.test.ts`에 회귀 가드를 추가했다. RED(`pnpm vitest run src/cli.test.ts` 1 failed)→GREEN(6 passed) 확인, `pnpm build`로 live runtime `dist/cli.js` 반영, `node dist/cli.js task --help`에서 item copy 확인, `pnpm check` green(TS/Vitest 54 files 288 tests, bridge pytest 449).
- `5202dbd test: clean stale delegated task wording`: USER_GUIDE 예시와 bridge regression test 이름/fixture에 남은 generic `delegate/delegated task` copy를 사람용 항목/조율 모델로 정리했다. 의도적 legacy regression/negative assertion/역사 문서 외 stale current-model grep hit는 남기지 않는다. docs/tests-only라 live 재시작 불필요. `pnpm check` green(TS/Vitest 54 files 287 tests, bridge pytest 449).
- `aebb6f9 fix: bound web graceful shutdown`: web child가 long-lived browser/WS 연결 때문에 SIGTERM graceful drain을 무한 대기할 수 있던 리스크를 닫았다. `WEB_GRACEFUL_SHUTDOWN_TIMEOUT_SECONDS=5`를 `uvicorn.run(timeout_graceful_shutdown=...)`에 연결하고 bridge regression을 추가했다. isolated child는 TERM 후 ~1s 종료, live old-code child는 TERM 후 stuck으로 KILL 필요, new-code live child는 TERM 후 ~6s self-exit하며 KILL 없이 wrapper가 재기동했다. live web 재배포 후 health 200, `/api/status running=4`, `/api/projects=[dev10]`, `/api/org default_assignee=grove-master`, panes 0/1/2/3, Slack heartbeat fresh 확인. `pnpm check` green(TS/Vitest 54 files 287 tests, bridge pytest 449). `0bccf28`에 handoff 기록.
- `19dd57a fix: stop synthesizing legacy project master nodes`: bridge web app에서 unreachable synthetic `project-master` helper/constant/append path를 제거했다. registry/org에 없는 legacy project-master를 web/API가 보정 생성하지 않도록 source-level guard를 추가했다. targeted org/master-chat tests, `test_web_app.py` 206 passed, `pnpm check` green(bridge pytest 448). 제품코드라 web만 재시작했고 live health/API green 확인. `5d57280`에 handoff 기록.
- `5d43e2e fix: align web e2e fixtures with lead nodes`: web e2e fixture/live harness가 더 이상 synthetic `project-master`를 만들지 않고 `lead -> worker` 모델을 사용한다. legacy cleanup line은 isolated fixture 정리용으로만 남긴다. `web/verify.mjs`에 project-master fixture negative assertion을 추가했다. `npm run verify`, `npm run typecheck`, `node e2e/tier1/runner.mjs` 7/7, `pnpm check` green. `7d16cf6`에 handoff 기록.
- `48ae091 test: align api fixtures with lead node model`: bridge API oracle/exhaustive/live coverage/web app tests와 web e2e API fixture의 generic `project-master`/`assign task` 현재모델 fixture를 `lead`/human-facing item 중심으로 정렬했다. explicit legacy assignee ignored regression은 남긴다. bridge targeted bundle 210 passed, `web npm run verify`, `pnpm check` green(bridge pytest 448). `8ed17f0`에 handoff 기록.
- `526ef33 docs: record consolidated live redeploy smoke`: 누적 web role-preset/assistant action spec/Slack control copy 변경을 consolidated web+Slack 재시작으로 live HEAD에 맞췄다. web started_at `1780694576`, Slack pid `59036`, socket_connected/heartbeat fresh, panes 0/1/2/3, `/api/status running=4`, `/api/projects=[dev10]`, `/api/org default_assignee=grove-master`, terminal ws-ticket 200 확인.
- `a9f8478 fix: describe Slack control commands as items`: Slack approve/abort control command의 preview/completed/denial reason에서 `approve task`, `approved task id`, `abort task`, `aborted task id`, `invalid task id` 등 사용자-facing task-id copy를 제거하고 item 중심 copy로 정렬했다. `tests/test_slack.py`에 approve/abort preview+confirm 회귀와 source-level stale copy guard를 추가했다. RED(`approved task id=...`, `abort task ...`)→GREEN(`uv run --group dev pytest tests/test_slack.py -q` 88 passed) 확인, `pnpm check` green(TS/Vitest 54 files 287 tests, bridge pytest 447).
- `d7766b3 fix: align assistant action specs with human items`: assistant action preview LLM schema를 `create_project|spawn_node|assign_item`으로 정렬하고, assignment target을 `existing human-facing item id`로 설명한다. `assign_task`/`delegate_task`는 외부/구모델 호환 alias로만 받아 `assign_item`에 정규화하며, persisted assistant_action metadata도 새 action type을 쓴다. notice system prompt의 `task id` 잔여를 `item id`로 교체했다. RED(`assign_item` 미지원, prompt에 `assign_task`/`delegate_task`/`task id` 노출)→GREEN(`uv run --group dev pytest tests/test_assistant.py -q`) 확인, `uv run --group dev mypy src tests`, `pnpm check` green(TS/Vitest 54 files 287 tests, bridge pytest 445).
- `e1bc858 fix: align web role preset coordination copy`: web NodeForm role preset preview의 sub-orchestrator 문구에서 `lead가 위임한 범위` 표현을 제거하고 `lead와 직접 합의한 범위`로 정렬했다. 사용자가 보는 role preset이 task/delegation protocol로 오해되지 않도록 `web/verify.mjs` static copy guard에 `위임한 범위`를 추가했다. RED(`node action copy must describe creating human-facing items, not delegated tasks`)→GREEN(`web npm run build && npm run verify && npm run typecheck`) 확인, `pnpm check` green.
- `b952901 fix: align init help with context scaffold`: `grove init --help`가 과거 `delegation-protocol doc`을 생성한다고 설명하던 stale CLI help를 `scaffold grove.yaml and grove-context.md`로 정정했다. `src/cli.test.ts`에 direct-org scaffold help regression을 추가했다. RED(`expected ... to contain scaffold grove.yaml and grove-context.md`)→GREEN(`pnpm vitest run src/cli.test.ts`, `pnpm build`, runtime `grove init --help`) 확인, `pnpm check` green(TS/Vitest 287 tests, bridge pytest 444).
- `6a591b6 fix: align web mock project defaults`: web mock/verify가 legacy `project-master`를 현재 모델처럼 다시 노출하지 않도록 정렬했다. assignee 후보에서 `project-master`를 제거하고, master-org/default facts는 mock root를, 새 프로젝트 생성 결과는 실제 API 계약과 같은 `lead`를 사용한다. `web/verify.mjs` core에 mock source/bundle의 `project-master` 잔여를 실패시키는 정적 가드를 추가했고, legacy full verify 경로에도 assignee/project creation/master chat negative assertion을 걸었다. RED(`web mock must not expose legacy project-master defaults`)→GREEN(`web npm run build && npm run verify`) 확인, `pnpm check` green.
- `44d505f fix: align project scaffolds with direct org model`: `grove init` 생성물을 v2 모델로 정렬했다. 새 파일은 `grove-context.md`이며 direct node communication, `grove org --json`, human-facing list item 모델을 설명한다. 더 이상 `grove-protocol.md`/delegation protocol/read-only reviewer scaffold를 생성하지 않는다. `grove new-project` 기본 lead role도 project board 중심 문구 대신 direct node communication + human-facing list items 모델을 주입한다. RED(`init`/`new-project` 테스트에서 stale copy 노출)→GREEN 확인, `pnpm build`로 runtime `dist/cli.js` 반영, `pnpm check` green.
- `da0227a docs: align current docs with human list model`: README, USER_GUIDE, skeleton current v2 섹션, LEAD-HANDOFF 현재 운영 섹션의 current/user-facing copy를 사람용 목록 모델로 정렬했다. board/task 명칭은 legacy backing API/역사 로그/negative assertion에만 남긴다. `pnpm check` green.
- `6093676 fix: avoid false zero node status while loading`: NodeStatusBar가 `/api/status` 첫 응답 전 `0 실행 중 / 0 전체`처럼 false zero를 표시하지 않도록 로딩 상태를 분리했다. mock harness에 status delay hook을 추가하고 `web/verify.mjs` core에서 RED(`statusLoading.loading=false`, `0 전체` 노출)→GREEN(`상태 불러오는 중`, no false zero, 응답 후 기존 `active -> running` alias counts 유지)을 확인했다. `web npm run build && npm run verify`, `web npm run typecheck`, `pnpm check` green. live web 재배포 후 Chrome request interception으로 `/api/status`를 지연시켜 로딩 중 false zero 없음, 응답 후 `4 실행 중 / 4 전체`, Slack pid `22392` heartbeat fresh를 확인했다.
- `8a230cb fix: hide connect from default cockpit`: 기본 web sidebar/command palette에서 `Connect` 표면을 제거했다. `?join=` 딥링크가 있을 때는 기존 `ConnectPanel`로 진입해 join code prefill과 URL scrub을 유지한다. `web/verify.mjs` core regression은 default sidebar에 `connect`가 없고 legacy/full 경로는 join 딥링크 중심으로 확인한다. live web 재배포 후 Chrome smoke에서 tabs `board/team/terminal/integrations/auth`, `connect` hidden, join deep-link visible/scrubbed, NodeStatusBar `4 실행 중 / 4 전체`, Slack pid `22392` heartbeat fresh를 확인했다. `web npm run build && npm run verify`, `web npm run typecheck`, `pnpm check` green.
- `46f1218 docs: clean internal board task wording`: bridge docstring, bridge test fixture/comment, web e2e assertion label에 남은 `board task(s)` 내부 표현을 `human-facing item/list item`으로 정리했다. `board task` 검색은 의도적인 negative assertion만 남는다. 영향 테스트 `tests/test_pull_executor.py`, `tests/test_web_app.py`, `tests/test_skeleton_bridge.py` 243 passed, 이후 `pnpm check` green.
- `0b50587 docs: align grove skills with human list items`: `AGENTS.md`, `skills-src/`, generated `skills/`, `.agents/skills/`의 grove skill 문구를 `board tasks`/`task-based communication` 대신 `human-facing list items` 모델로 정렬했다. `python3 scripts/generate_grove_skills.py`로 target surfaces를 재생성했고, deprecated copy `rg` 결과 없음, `pnpm check:skills && pnpm format:check`, `pnpm check` green.
- `832b2a2 fix: describe context packs as human list items`: TypeScript/Python context pack 기본 communication protocol과 master Slack/web chat system prompt에서 `Board tasks are...` 문구를 제거하고 `Human-facing list items are...`로 정렬했다. TS/Python context pack tests와 assistant prompt regression을 RED→GREEN으로 추가했다. `pnpm check` green. `/opt/homebrew/bin/grove`가 `dist/cli.js`를 직접 쓰므로 `pnpm build`도 실행해 runtime CLI dist에 같은 문구가 반영됐는지 확인했다. live web 재배포와 Slack pane respawn 후 web health 200, `/api/status running=4`, `/api/boards=[dev10 task_count=0]`, Slack pid `22392` `socket_connected` heartbeat fresh, panes `dev10:0/1/2/3` 확인.
- `dbbd742 fix: describe role preset items as human lists`: CLI canonical role preset과 web NodeForm preset preview에서 `보드 task` 문구를 제거하고 `사람용 목록 항목` 모델로 정렬했다. `src/role-presets.test.ts`와 `web/verify.mjs`에 회귀 가드를 추가했다. RED(`보드 task` 노출) 확인 후 `pnpm vitest run src/role-presets.test.ts`, `web npm run build && npm run verify`, `pnpm check` green. live web 재배포 health 200, Slack pid `84491` 유지 및 heartbeat fresh 확인.
- `b6aa03d fix: describe executor prompts as human items`: pull executor가 노드에 넘기는 프롬프트 헤더를 `grove board task 실행`이 아니라 `grove human-facing item 작업`으로 정렬했다. env 변수명은 호환성 때문에 유지했다. RED(`You are executing a grove board task`) 확인 후 pull executor regression, `tests/test_pull_executor.py` 34 passed, `pnpm check` green.
- `f1070ae fix: describe master chat counts as human items`: master chat fallback/fact summary와 web mock answer에서 board count를 `Board tasks`가 아니라 `Human items`로 설명한다. board를 노드 간 통신 프로토콜처럼 보이게 하는 잔여 copy를 제거했다. RED(`Board tasks:` 노출) 확인 후 bridge regression test 추가, `web npm run build && npm run verify`, `pnpm check` green, live web 재배포 health 200.
- `99386c1 test: guard active node liveness in web mock`: live product fix 이후 mock `/api/status`도 registry `active`를 `running`으로 정규화하게 맞췄다. core `web/verify.mjs`가 상태를 `active`로 바꾼 뒤 NodeStatusBar가 `3 실행 중/2 대기`로 바뀌는지 확인한다. RED(`2 실행 중/3 대기` 고정) 확인 후 fix, `web npm run build && npm run verify && npm run typecheck`, `pnpm check` green.
- `42ad5c4 fix: normalize active nodes in summary counts`: summary export/aggregate의 node `by_status`에서도 `active`를 `running`으로 정규화한다. 외부 리포트에서 active 서비스 노드가 `other`로 빠지는 것을 막는다. bridge summary privacy test와 `pnpm check`, live web 재배포 health 200 확인.
- `c954317 fix: classify active nodes as live in status`: `/api/status`가 registry `active` 서비스 노드를 idle로 분류해 상단 NodeStatusBar가 `running=1/idle=3`처럼 보이던 문제를 고쳤다. summary와 detail 모두 `active`를 running/live 계열로 정규화한다. bridge regression test, `pnpm check`, live API/DOM smoke green.
- `2faab47 fix: expose pane exists in web node payloads`: CLI `grove org --json`와 동일하게 web API `/api/nodes`, `/api/org`, project creation `project_master` payload에도 `pane_exists`를 명시 노출한다. node-count/status만으로 pane 소실을 놓치지 않도록 외부/웹 소비자가 같은 liveness 신호를 쓸 수 있다. bridge tests와 web type을 갱신했고 `pnpm check`, live API smoke green.
- `5d7bd69 fix: count live nodes by pane availability`: sidebar live count가 `status==="running"`만 세서 live dev10의 `active` 서비스 노드들을 누락하던 문제를 고쳤다. terminal pane이 있고 `dead/error/stale`가 아닌 노드를 live로 세며, `active` 상태 label/dot도 사용자-facing `활성/active`로 정렬했다. core verify에 `4/6` live count 회귀 가드를 추가했고, live web smoke에서 `4/4 실시간`을 확인했다.
- `d8b021b fix: avoid empty org flash while loading`: Team 패널이 `/api/org` 응답 대기 중 초기 `nodes=[]`를 실제 빈 조직처럼 보여 `노드가 없습니다`/0-node로 오인되던 문제를 고쳤다. `OrgChart`에 loading 상태를 분리하고 `web/verify.mjs` 기본 core verify에 delayed org response 회귀 가드를 추가했다. `web npm run build && npm run verify`, `pnpm check`, live web 재배포, live delayed-org Chrome smoke green.
- `2d63562 fix: align web item assignment copy`: web Team/Planner의 `위임/Delegate task/Assign task` 사용자 문구를 사람용 항목 생성·배정 copy로 정렬했다. `web/verify.mjs`에 해당 회귀 가드를 추가했고, `web npm run build && npm run verify`, `pnpm check`, live web 재배포 health 200을 확인했다.
- `a5305b6 fix: match tmux pane liveness exactly`: tmux target fuzzy matching 때문에 `dev10:2.0`이 없어도 `display-message`가 `dev10:1.0`을 반환하던 오인을 제거했다. CLI/bridge 모두 `tmux list-panes -a -F "#{session_name}:#{window_index}.#{pane_index}"` 결과와 완전 일치로 pane 존재를 판단한다. `pnpm check` green, live CLI build 및 web 재배포 완료.
- `bd9d6f7 fix: detect missing tmux panes in org views`: registry node가 active여도 실제 tmux pane이 사라지면 `grove org`와 `/api/org`/`/api/nodes`에서 missing 상태로 노출한다. advisor가 제기한 "node-count로 pane 소실 감지 불가" 리스크를 닫기 위한 1차 fix였고, 이후 `a5305b6`에서 fuzzy matching까지 교정했다.
- `0f0ae07 fix: describe Slack intake as human-facing item`: Slack intake preview/confirm/denial/audit copy를 `task`가 아니라 사람용 item 생성으로 정렬했다. live Slack은 pid `84491`로 재시작되어 해당 코드가 반영됐다.
- `277964c fix: keep Slack human decision copy item-focused`: Slack 사람 판단 필요 안내에서 `task unblock` copy와 completed notice reason의 `task_unblocked` 표현을 제거하고, 사람 판단 항목에 답변을 남기는 copy로 정리했다. `tests/test_slack.py`에 회귀 가드를 추가했고 `pnpm check` green. live Slack은 코드 반영을 위해 재시작했고 `dev10:2.0` pane 복구, pid `79225`, `socket_connected`, heartbeat fresh를 확인했다.
- `6a23595 fix: keep inbox answer copy item-focused`: Decision inbox 답변 UI와 `/api/inbox.answer` metadata에서 `unblock task/board events` 중심 copy를 제거하고, 사람 판단 항목에 답변을 남기는 모델로 정리했다. web verify와 bridge pytest에 회귀 가드를 추가했고, live web 재배포 후 health 200, Slack heartbeat, dev10-only project/board, terminal WS frame 수신을 확인했다.
- `23fca58 fix: describe delegate as human-facing item`: `grove delegate` CLI/help/error/warning/test copy를 node-to-node delegated task가 아니라 사람용 TODO/feedback item 생성으로 정렬했다. `pnpm check` green.
- `33418d0 fix: remove read-only copy from Slack intake UI`: 기본 Slack integration 화면의 intake 안내에서 `읽기 전용/read-only` 표현을 제거하고 core verify에 회귀 가드를 추가했다. `pnpm check` green.
- `29a2978 fix: allow master terminal input in web mock`: web terminal mock/verify를 v2 모델에 맞춰 root/master pane도 입력 가능하게 정렬하고, 터미널 라벨에서 `읽기 전용/read-only` 노출을 `실시간 미러/live mirror`로 교체했다. `npm run check`(web)와 `pnpm check` green.
- `49564c2 fix: keep master fallback copy user-facing`: MASTER deterministic fallback/feedback preview 문구에서 `read-only`, future route, internal module name 같은 구현 중심 표현이 사용자에게 새지 않도록 정리하고 회귀 테스트를 추가했다. `pnpm check` green.
- `cc0ec74 fix: align role preset copy with v2 org model`: web 노드 생성 role preset preview에서 reviewer/QA/maker 능력을 제한처럼 보이게 하던 copy를 제거하고, v2의 자유 작업/직접 검증 모델에 맞췄다. live web 재배포 후 health 200, org/board/Slack 정상 확인.
- `defeb46 fix: keep Slack human-decision notice user-facing`: Slack의 사람 판단 필요 알림이 assistant notice에 내부 `human gate requested...` 문구를 넘기지 않고, 사용자-facing `Human decision needed...` 문구를 넘기도록 했다. live Slack은 재시작 후 `socket_connected`와 heartbeat를 확인했다.
- `137d66f fix: scope board listing to active project`: 기본 세션 `/api/boards`도 현재 project board로 스코프를 걸어 과거/다른 프로젝트 board가 UI 선택지에 섞이지 않게 했다. 이후 `dev-room` legacy compatibility endpoint는 직접 접근만 유지하고 `/api/boards` 목록에는 active project board만 노출하도록 정렬했다.
- `e042622 fix: guard node-routed master chat`: isolated e2e/temp server가 실제 `grove-master` pane으로 `grove ask`를 보내지 않도록, node-routed master chat은 target node가 현재 `GROVE_HOME` registry에 terminal-viewable로 있을 때만 CLI를 호출한다.
- `5dec69d fix: expose remote web companion urls`: `~/.grove/dev10/web.json`에 `allowed_hosts`와 `remote_urls`를 기록해 remote/headless 접속 정보를 노드가 직접 확인할 수 있게 했다.
- `7100279 fix: prefer master terminal by default`: terminal view 첫 진입 시 raw node 배열 순서가 아니라 `grove-master`/lead/root 우선순위로 기본 pane을 선택한다.
- `407c84e docs: refresh live dev10 handoff`: live dev10 운영 상태를 문서화했다.
- `c5a037a fix: keep grove master as default assignee`: advisor가 생겨도 `/api/org` default assignee는 `grove-master`로 유지한다.
- `3e9e739 fix: report live slack socket heartbeat`: Slack socket heartbeat를 runtime 파일로 기록하고 `/api/slack/config/status`에 반영한다.

과거 스켈레톤 갭 완료 기록:

- **PR-E 역할 프리셋**(2dfc8b2): `src/role-presets.ts`, spawn `--role-preset`, web NodeForm preset select.
- 과거 **PR-F 조직/업무방식 context prepend**(2668c03): `src/context-pack.ts`+`bridge/.../context_pack.py`가 dispatch 전 context를 prepend했다. 현재는 보드 task 중심 통신 모델이 폐기됐고, context pack은 org/pane/cwd 인지 보조로만 해석한다.
- **test coverage**(3518dd9): bridge API + core 단위 + Playwright e2e 스캐폴드. vitest를 `src/**/*.test.ts`로 스코핑(playwright .spec 오실행 해소).
- **slack cold-channel fix**(4f3c804, cherry-pick): cold message.channels 게이팅. 이후 engaged-thread 자동응답 제거와 Slack runtime heartbeat까지 추가됐고, 현재 Slack은 사용자 승인 후 `grove-master` 직통으로 켜져 있다.

## 2. 진행 중 (in-flight)

- 현재 별도 worker in-flight는 없다. live 운영은 `grove-master`가 직접 관리하고, advisor가 5분마다 비차단 점검한다.
- 계속할 작업은 장시간 steady-state 검증과 작은 회귀 제거다. 우선순위: web 재시작 루프 유지, Slack heartbeat/소켓 freshness 유지, remote terminal ticket 200 유지, `/api/projects` 단일 dev10 유지, `/api/org` default assignee `grove-master` 유지.
- 새 프로젝트/노드 생성, 조직도 변경, 노드 종료는 사람이 명시 지시할 때만 operator 경로로 수행한다.

## 3. 통합 규율 (worktree)

worktree(`~/grove-worktrees/{auth,gui,master,slack,platform}`)는 main보다 크게 stale해질 수 있음(46~58커밋 사례). **raw merge 금지** — 작성자가 현재 main에 rebase(자기 델타만 replay)→충돌 양쪽 보존→full `pnpm check` green→lead가 `git merge --ff-only`(또는 무충돌 cherry-pick). 실제 델타는 `git show <tip> --stat`로 확인(tip-to-tip diff는 staleness 착시로 대량 삭제처럼 보임). 다중 노드가 공유 main 체크아웃에 쓰면 WIP가 게이트 오염 → 노드 커밋 금지, lead가 단일 통합 커밋.

## 4. 데몬/노드

- web: `dev10:1.0`, `0.0.0.0:8765`, tailnet `100.100.90.87`, LAN `192.168.1.186`, `/Users/chopin/.grove/dev10/run-web-loop.sh`가 재시작 루프를 담당한다.
- slack: `dev10:2.0`, `/Users/chopin/.grove/dev10/run-slack-loop.sh`가 재시작 루프를 담당한다.
- grove-master: `dev10:0.0`, codex, root.
- advisor: `dev10:3.0`, claude, `grove-master`의 advisory child. 5분 점검 루프를 유지한다.
- 조직도 `roots:['grove-master']`. 기본 live 프로젝트는 `dev10` 하나다.

## 5. 캐논 스켈레톤 (최우선 미션)

`GROVE MASTER → 프로젝트 lead → 프로젝트별 조직`. 8항 + 갭 스펙 = `docs/agents/skeleton-gap-implementation.md`. 미션: 갭 전부 구현 → 철저 e2e/API → TRUE stable, 무한 반복.
