{
  "name": "@grove/grove-viewer-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Frontend for the grove-viewer Legacy dashboard plugin (Dev Room). Self-contained build/check — intentionally NOT part of grove's root pnpm workspace or `pnpm check`.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "verify": "node verify.mjs",
    "check": "npm run typecheck && npm run build && npm run verify"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "esbuild": "^0.27.7",
    "puppeteer-core": "^23.11.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.3"
  }
}
